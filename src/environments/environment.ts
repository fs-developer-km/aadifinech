export const environment = {
  production: true,
  apiBaseUrl: 'https://api.aadifintech.com/api'  // when deployed (e.g. AWS)
  // apiBaseUrl: 'https://aadifintech-backend.onrender.com/api'  // when deployed (e.g. AWS)
  // apiBaseUrl: 'https://api.aadifintech.com/api'  // when deployed (e.g. AWS)
};
